import { FieldValidationErrorDirective } from './field-validation-error.directive';

describe('FieldValidationErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldValidationErrorDirective();
    expect(directive).toBeTruthy();
  });
});
